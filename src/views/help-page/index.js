import { Typography } from '@mui/material';
import MainCard from 'ui-component/cards/MainCard';

const HelpPage = () => (
    <MainCard title="帮助信息">
        <Typography variant="body2">这里是帮助信息</Typography>
    </MainCard>
);

export default HelpPage;
